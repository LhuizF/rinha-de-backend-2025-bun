{
  "name": "app",
  "version": "0.1.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run --optimize --max-connections=2000 src/index.ts",
    "start:worker": "bun run src/worker.ts"
  },
  "dependencies": {
    "ioredis": "^5.6.1",
    "p-queue": "^8.1.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/pg": "^8.15.4",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
